# Rasyn Chemistry OS — AWS Deployment Configuration
# Copy this to config.env and fill in your values:
#   cp config.env.example config.env

# AWS region to deploy in
AWS_REGION=us-east-1

# Environment name (used as prefix for all resources)
ENVIRONMENT=rasyn-prod

# EC2 GPU instance type
# Options: g4dn.xlarge ($0.53/hr), g5.xlarge ($1.01/hr), g5.2xlarge ($1.21/hr)
INSTANCE_TYPE=g5.xlarge

# S3 bucket for model checkpoints (must be globally unique)
# Leave empty to auto-generate: rasyn-models-<account-id>
S3_BUCKET=

# EC2 key pair name for SSH access
# Leave empty to be prompted during deploy
KEY_PAIR_NAME=

# Path to model checkpoints on your LOCAL machine (optional)
# If set, deploy.sh uploads them to S3 automatically
# If empty, upload manually or use upload-models-from-pod.sh on RunPod
CHECKPOINT_DIR=
WEIGHTS_DIR=

# --- What gets deployed on the EC2 instance ---
# 1. PostgreSQL 16 with pgvector extension
# 2. Redis 7 (task broker + pub/sub for SSE)
# 3. FastAPI API server (uvicorn, port 8000)
# 4. Celery worker (GPU inference, concurrency=1)
# 5. Models downloaded from S3 on first boot
